int arr[] = { 1,2,3,4,5 };
int (*p)[5];				//定义了一个指针数组，它包含5个int类型的数组
					//若是将5改为3，当执行时(*p)[3],(*p)[4].... 时会导致越界访问
					//若是将5改成6，在访问过程中会偏离arr实际的内存分布，可能访问不属于arr的内存区域......
p = &arr;
for (int i = 0;i < 5;i++)
{
    printf("%d\n",(*p)[i]);     		//C
    std::cout << (*p)[i] <<"\n";   	//C++
}